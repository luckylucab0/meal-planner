// Settings — CalDAV-Credentials (Apple) und Feature-Flags. Wird in Schritt 12 implementiert.

export default function Settings() {
  return (
    <section>
      <h1 className="text-2xl font-semibold">Einstellungen</h1>
      <p className="mt-2 text-neutral-500">CalDAV-Sync mit iCloud und sonstige Feature-Flags.</p>
    </section>
  );
}
