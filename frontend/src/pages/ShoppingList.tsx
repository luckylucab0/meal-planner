// Einkaufsliste — nach Kategorien, druckbar, mit Checkboxen (LocalStorage).
// Wird in Schritt 9 implementiert.

export default function ShoppingList() {
  return (
    <section>
      <h1 className="text-2xl font-semibold">Einkaufsliste</h1>
      <p className="mt-2 text-neutral-500">
        Aggregierte Zutaten gruppiert nach Schweizer Supermarkt-Layout.
      </p>
    </section>
  );
}
