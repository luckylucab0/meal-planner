"""ORM-Modell `products` — Zutaten-Cache (Open Food Facts + Agent + manuell).

Quelle wird in `source` festgehalten ('off' / 'agent' / 'manual'), OFF-Treffer
zusätzlich mit `off_barcode` und `off_fetched_at`. Wird in Schritt 3 implementiert.
"""
